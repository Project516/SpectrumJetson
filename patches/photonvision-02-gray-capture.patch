diff --git a/photon-core/src/main/java/org/photonvision/vision/frame/provider/USBFrameProvider.java b/photon-core/src/main/java/org/photonvision/vision/frame/provider/USBFrameProvider.java
index 85d461ca..a8885521 100644
--- a/photon-core/src/main/java/org/photonvision/vision/frame/provider/USBFrameProvider.java
+++ b/photon-core/src/main/java/org/photonvision/vision/frame/provider/USBFrameProvider.java
@@ -26,6 +26,7 @@ import org.opencv.core.Mat;
 import org.photonvision.common.logging.LogGroup;
 import org.photonvision.common.logging.Logger;
 import org.photonvision.jni.CscoreExtras;
+import org.photonvision.vision.frame.FrameThresholdType;
 import org.photonvision.vision.opencv.CVMat;
 import org.photonvision.vision.processes.VisionSourceSettables;
 
@@ -35,6 +36,13 @@ public class USBFrameProvider extends CpuImageProcessor {
     private UsbCamera camera = null;
     private CvSink cvSink = null;
 
+    // SpectrumJetson: when the pipeline wants GREYSCALE (AprilTag, AprilTagCuda,
+    // ArUco), grab from a kGray sink so cscore decodes MJPEG straight to 1-channel
+    // gray, skipping chroma upsampling, color conversion and the later BGR->gray
+    // cvtColor. Other pipelines (HSV, calibration, driver mode) keep the BGR sink.
+    private CvSink graySink = null;
+    private volatile boolean useGray = false;
+
     @SuppressWarnings("SpellCheckingInspection")
     private VisionSourceSettables settables;
 
@@ -84,10 +92,11 @@ public class USBFrameProvider extends CpuImageProcessor {
             // Hal::initialize was called
             // TODO - under the hood, this incurs an extra copy. We should avoid this, if we
             // can.
-            long captureTimeNs = cvSink.grabFrame(mat.getMat(), CSCORE_DEFAULT_FRAME_TIMEOUT) * 1000;
+            CvSink sink = activeSink();
+            long captureTimeNs = sink.grabFrame(mat.getMat(), CSCORE_DEFAULT_FRAME_TIMEOUT) * 1000;
 
             if (captureTimeNs == 0) {
-                var error = cvSink.getError();
+                var error = sink.getError();
                 logger.error("Error grabbing image: " + error);
             }
 
@@ -97,25 +106,26 @@ public class USBFrameProvider extends CpuImageProcessor {
             // TODO - consider a frame pool
             // TODO - getCurrentVideoMode is a JNI call for us, but profiling indicates it's fast
             var cameraMode = settables.getCurrentVideoMode();
+            CvSink sink = activeSink();
+            boolean gray = sink == graySink;
             var frame = new RawFrame();
             frame.setInfo(
                     cameraMode.width,
                     cameraMode.height,
-                    // hard-coded 3 channel
-                    cameraMode.width * 3,
-                    PixelFormat.kBGR);
+                    cameraMode.width * (gray ? 1 : 3),
+                    gray ? PixelFormat.kGray : PixelFormat.kBGR);
 
             // This is from wpi::Now, or WPIUtilJNI.now(). The epoch from grabFrame is uS since
             // Hal::initialize was called
             long captureTimeUs =
                     CscoreExtras.grabRawSinkFrameTimeoutLastTime(
-                            cvSink.getHandle(), frame.getNativeObj(), CSCORE_DEFAULT_FRAME_TIMEOUT, lastTime);
+                            sink.getHandle(), frame.getNativeObj(), CSCORE_DEFAULT_FRAME_TIMEOUT, lastTime);
             lastTime = captureTimeUs;
 
             CVMat ret;
 
             if (captureTimeUs == 0) {
-                var error = cvSink.getError();
+                var error = sink.getError();
                 logger.error("Error grabbing image: " + error);
 
                 frame.close();
@@ -141,6 +151,35 @@ public class USBFrameProvider extends CpuImageProcessor {
         CameraServer.removeServer(cvSink.getName());
         cvSink.close();
         cvSink = null;
+        synchronized (this) {
+            if (graySink != null) {
+                graySink.close();
+                graySink = null;
+            }
+            useGray = false;
+        }
+    }
+
+    private CvSink activeSink() {
+        CvSink g = graySink;
+        return useGray && g != null ? g : cvSink;
+    }
+
+    @Override
+    public void requestFrameThresholdType(FrameThresholdType type) {
+        super.requestFrameThresholdType(type);
+        boolean gray = type == FrameThresholdType.GREYSCALE;
+        synchronized (this) {
+            if (cvSink == null || gray == useGray) return;
+            if (gray && graySink == null) {
+                graySink = new CvSink("opencv_gray_" + camera.getName(), PixelFormat.kGray);
+                graySink.setSource(camera);
+            }
+            if (graySink != null) graySink.setEnabled(gray);
+            cvSink.setEnabled(!gray);
+            useGray = gray;
+            logger.info("Capture format: " + (gray ? "MJPEG->gray (kGray sink)" : "BGR"));
+        }
     }
 
     @Override
diff --git a/photon-core/src/main/java/org/photonvision/vision/pipe/impl/GrayscalePipe.java b/photon-core/src/main/java/org/photonvision/vision/pipe/impl/GrayscalePipe.java
index 97b95b19..9d283e28 100644
--- a/photon-core/src/main/java/org/photonvision/vision/pipe/impl/GrayscalePipe.java
+++ b/photon-core/src/main/java/org/photonvision/vision/pipe/impl/GrayscalePipe.java
@@ -25,6 +25,11 @@ public class GrayscalePipe extends CVPipe<Mat, Mat, GrayscalePipe.GrayscaleParam
     @Override
     protected Mat process(Mat in) {
         var outputMat = new Mat();
+        if (in.channels() == 1) {
+            // SpectrumJetson: already gray (kGray capture sink); just copy.
+            in.copyTo(outputMat);
+            return outputMat;
+        }
         // We can save a copy here by sending the output of cvtcolor to outputMat directly
         // rather than copying. Free performance!
         Imgproc.cvtColor(in, outputMat, Imgproc.COLOR_BGR2GRAY, 3);
